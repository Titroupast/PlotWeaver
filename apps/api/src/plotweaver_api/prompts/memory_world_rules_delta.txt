[任务]
你要基于“现有世界规则 + 本章正文 + 评审结果”，提取**仅本章新增或修正**的世界规则增量。

[现有世界规则 JSON]
{existing_world_rules_json}

[本章正文]
{chapter_text}

[评审结果 JSON]
{review_json}

[输出要求]
- 只输出 Markdown 有序列表（每行一条），不要输出解释文字。
- 每条规则必须是“可执行/可验证”的约束语句，不要写剧情复述。
- 严禁原文大段摘抄，单条不超过 40 字。
- 只写新增或发生变化的规则；若无新增，输出：`1. 无新增世界规则。`
- 最多输出 8 条。
