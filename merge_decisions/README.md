# Merge Decisions

这里存放人工合并决策（merge decision）的文件化记录，主要用于处理同名角色、化名/马甲、身份拆分等情况。

建议内容（Phase 1 最小）：
- `<decision_id>.json`：决策文件（包含 decision_type、目标 `character_id`、理由、操作者、关联 run/chapter）。
- 字段说明与示例：以 `SPEC.md` 的 8.4.1 为准。
